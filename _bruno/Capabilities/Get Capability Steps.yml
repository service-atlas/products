info:
  name: Get Capability Steps
  type: http
  seq: 9

http:
  method: GET
  url: "{{baseUrl}}/capabilities/:id/steps"
  params:
    - name: id
      value: "1"
      type: path
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "200"
    - expression: res.body
      operator: isArray

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: example
    request:
      url: "{{baseUrl}}/capabilities/:id/steps"
      method: GET
      params:
        - name: id
          value: "1"
          type: path
    response:
      status: 200
      statusText: OK
      headers:
        - name: content-type
          value: application/json
        - name: vary
          value: Origin, Accept-Encoding
        - name: date
          value: Wed, 15 Jul 2026 00:00:06 GMT
        - name: content-length
          value: "151"
      body:
        type: json
        data: |-
          [
            {
              "id": 2,
              "capability_id": 1,
              "flow_step_id": 5,
              "protocol": "HTTP",
              "target": "/api/boats",
              "created_at": "2026-07-14T19:00:02.741877-05:00",
              "updated_at": "2026-07-14T19:00:02.741877-05:00"
            }
          ]

docs: |-
  # Get Capability Steps

  Get all steps for a capability

  ## Possible Status Codes
  | Status Code | Notes |
  | -- | --|
  | 200 | Created |
  | 400 | Invalid id |
  | 404 | Capability not found |
  | 500 | Internal server error |
